# Build Docker image
    docker build -t my-node-app .

# Run Docker container
    docker run -d -p 3000:3000 --name docker-node-app my-node-app

# Stop Docker container
    docker stop docker-node-app

# Remove Docker container
    docker rm docker-node-app
